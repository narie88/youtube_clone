import React from "react";
import SearchForm from "./searchForm";

const SearchBar = () => {
  return (
    <div>
      <a href="#none">로고</a>
      <SearchForm />
    </div>
  );
};

export default SearchBar;
